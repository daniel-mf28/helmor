---
"helmor": minor
---

Show the running local LLM in the composer model picker, so a model loaded from the Local LLM settings panel can now be selected for a session instead of only powering session titles and Voice Pilot.
